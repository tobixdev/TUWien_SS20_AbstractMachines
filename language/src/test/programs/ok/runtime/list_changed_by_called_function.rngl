# 9
fn main():
    let lst = [1,2,3,4,5,6,7,8,9]
    let x = empty_list(lst)
    ret len(lst)

fn empty_list(lst):
    while lst matches [a.tail]:
        lst = tail

fn len(lst):
    ret eval:
        lst matches []: 0
        lst matches [a.tail]: 1 + len(tail)